---
import Layout from "./Layout.astro";
const { frontmatter } = Astro.props;
---

<Layout title={frontmatter.title}>
    <article class="prose prose-grey prose-sm prose-p:leading-normal prose-h2:text-accent prose-headings:mt-6">
        <slot />
    </article>
</Layout>
